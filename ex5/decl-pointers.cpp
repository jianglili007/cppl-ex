// Exercise 1

// Pointer to char.
char* pc = &c;

// Array of 10 ints.
int ai[10] = {0}; // Initialize to all 0's.

// A reference to an array of 10 ints.
int& rai = ai;

// Pointer to array of strings.
int* psa = &sa; // Assuming there's a string sa[] somewhere.

// Pointer to a pointer to a character.
char** ppc = pc; // pc points to a character; not really sure of this one.

// A const int.
const int const_int;

// Pointer to a const int.
const int* pci = &const_int;

// A constant pointer to an int
int *const cpi = &non_const_int;
